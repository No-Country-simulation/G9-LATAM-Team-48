# Blueprint Render — solo el microservicio ML (backend Railway, front Vercel).
services:
  - type: web
    name: g9-latam-ml
    rootDir: ml-service
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
