# ============================================================================
# Plantilla de variables de entorno para desarrollo local
# SÍ VERSIONABLE - ejemplo de configuración recomendada
# ============================================================================
# Copia estos valores a:
# - Run > Edit Configurations > [Spring Boot config] > Environment variables
# - O exporta estas variables en tu terminal antes de ejecutar

DB_URL=jdbc:mysql://localhost:3306/energia_ai?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=energia_app
DB_PASSWORD=change-me
