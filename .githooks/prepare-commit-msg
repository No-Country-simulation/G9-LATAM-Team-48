#!/bin/sh
# Quita co-autoría de Cursor inyectada por el IDE (Team 48).
MSG_FILE=$1
[ -f "$MSG_FILE" ] || exit 0

tmp="${MSG_FILE}.energia-strip"
grep -viE 'Co-authored-by:[[:space:]]*Cursor[[:space:]]*<' "$MSG_FILE" > "$tmp" 2>/dev/null || cp "$MSG_FILE" "$tmp"
mv "$tmp" "$MSG_FILE"
exit 0
