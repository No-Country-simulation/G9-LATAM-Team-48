# Sincronización OCI → local (2026-08-16)

Revisión de cambios hechos **en la VM OCI** (`opc@163.176.248.56`) que aún no estaban en Git, cruzados con el repo local (`Jorge-martinez`).

## Estado encontrado en OCI

| Pieza | Estado |
|-------|--------|
| Rama git | `Jorge-martinez` en `c010029` (V15) — **atrás** del remoto (falta V16 + commits front) |
| Working tree | Modificados sin commit: Dao, `application-prod.yml`, `docker-compose*.yml` |
| Runtime real | **systemd**: `energy-backend.service` (jar) + Podman `energyai-db` / `container-energyai-db.service`. Health `UP`. Contenedores `energia_*` del compose experimental **no** están en uso |

No se trajeron dumps, jars ni backups (`backup_*.sql`, `deploy/app.jar`, CSV).

---

## Qué se adoptó en el repo local

### 1. `DatasetFeatureEngineeringDao` (sí)

Cambio útil de OCI, integrado sin el bug de caché:

- `hasRows()` también detecta filas en `dataset_dashboard_monthly`.
- `useRollups()` usa `SELECT 1 … LIMIT 1` en lugar de `COUNT(*)` (más barato en cold-check).
- Se **mantiene** el caché de `rollupsReady == false` (la versión OCI solo cortocircuitaba `TRUE` y reconsultaba siempre cuando no había rollups).

### 2. `application-prod.yml` — solo Hikari (sí, parcial)

Se agregó pool Hikari configurable por env (`HIKARI_*`) sobre el perfil prod **completo** (JWT, mail, prediction, Flyway, actuator mail off).

**No** se adoptó el `application-prod.yml` completo de OCI porque:

- Quitaba JWT / mail / prediction / `FRONTEND_BASE_URL` / Google.
- Ponía defaults de password en el YAML.
- Activaba `hikari` DEBUG a nivel logging.
- Rompía el contrato `DB_URL` / `DB_USERNAME` / `DB_PASSWORD` que usa el compose actual.

### 3. `docker-compose.oci.yml` / `docker-compose.yml` de OCI (no)

Rechazados para merge tal cual. Motivos:

| Cambio OCI | Riesgo |
|------------|--------|
| Rename `db` → `mysql`, volúmenes y container names | Pierde volumen `energyai_mysqldata` / rompe deploy documentado |
| `3306:3306` público | Expone MySQL a Internet |
| Env `SPRING_DATASOURCE_*` sin `DB_*` + sin mail/JWT/Google | Backend no arranca o pierde correo / auth |
| `JPA_DDL: update` en compose local | Schema fuera de Flyway |
| Passwords en defaults del compose | Secretos en repo |
| Quitar servicios `ml` / `frontend` del compose NAS | Rompe flujo local/NAS documentado |

El compose canónico sigue siendo el de Git + UTF-8 JDBC (`characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci`) ya trabajado en local.

---

## Qué quedó pendiente / recomendado en OCI

1. **Restaurar** en la VM los YAML trackeados a `HEAD` (o al backup `docker-compose.oci.yml.local.bak`) para no redeployar accidentalmente el compose incompleto:
   ```bash
   cd ~/G9-LATAM-Team-48
   git checkout -- docker-compose.oci.yml docker-compose.yml backend/src/main/resources/application-prod.yml
   ```
2. **`git pull origin Jorge-martinez`** cuando se quiera alinear V16 + front (hoy la VM está en V15).
3. **No** migrar a compose `energia_*` ni `docker-compose … --force-recreate`: prod estable es **systemd** (`energy-backend.service` + `energyai-db`).
4. Redeploy futuro = `mvn package` + `sudo systemctl restart energy-backend.service` (ver `DEPLOY_PRODUCCION.md`).
5. No commitear dumps/backups de la home OCI.

---

## Trabajo local relacionado (misma ventana)

Además del sync OCI, en local ya había:

- Seed SQL categorías form (`insulation`, `occupancy`, `building`, `peak`, `zone`) + i18n 21 locales.
- Mapper / order de categorías.
- JDBC UTF-8 en `.env.example` y compose.

Ver: `backend/scripts/sql/seed_recommendation_catalog_form_categories.sql`, `docs/backend/RECOMMENDATION_V2.md`.

---

## Checklist post-merge (cuando se haga commit/push)

- [ ] `mvn -q -Dtest=… test` o al menos compile backend
- [ ] Smoke: `/actuator/health`, `/api/consumos`, `/api/recomendaciones?nivel=moderate&domain=insulation`
- [ ] No incluir `_oci_*` ni passwords en el commit
- [ ] Push `Jorge-martinez` + sync `backend`
- [ ] En OCI: restaurar compose, pull, redeploy según modo jar/Podman
